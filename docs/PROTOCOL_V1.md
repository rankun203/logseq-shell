# logseq-shelld Protocol v1 (placeholder)

This file will define the exact WebSocket message schema for:

- session creation
- input/output streaming
- resize
- lifecycle events
- errors and reconnect semantics

Current high-level protocol is documented in `REPO_BLUEPRINT.md` section 4.
